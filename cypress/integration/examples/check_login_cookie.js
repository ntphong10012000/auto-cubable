describe('Login', () => {
    beforeEach(() => {
      cy.login();
    });
  
    it('should verify successful login', () => {
      // Kiểm tra sau khi đăng nhập thành công
      // ...
    });
  
    // Các test case khác sau khi đã đăng nhập
  });
  